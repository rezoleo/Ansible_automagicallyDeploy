# Host example for the create-ansible-user playbook
host-1 ansible_user=<non-root-user> ansible_password=<non-root-password> ansible_become_password=<root-password> ansible_become_method=su

# Host example once configured with the Ansible user
host-2 ansible_user=ansible ansible_ssh_private_key=id_ed25519_ansible
